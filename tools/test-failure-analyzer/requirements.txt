# Test Failure Analyzer Dependencies

# GitHub API access
requests>=2.31.0

# YAML parsing (for deployment manifests)
pyyaml>=6.0

# Date/time parsing (used by analyzer.py)
python-dateutil>=2.8.0

# Rich console output (used by analyzer.py)
rich>=13.0.0

# Data validation (used by schemas.py)
pydantic>=2.0.0

# LLM Analysis (Phase 8)
# Google Gemini API (default, recommended)
google-generativeai>=0.3.0

# OpenAI API (alternative provider)
openai>=1.0.0

# Optional: Kubernetes API access (if needed for live pod querying)
# kubernetes>=29.0.0

# AWS SDK (Agent execution on ECS: update DynamoDB heartbeat/status, upload artifacts to S3)
boto3>=1.34.0

